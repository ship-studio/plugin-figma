---
phase: 06-brief-instructions-terminology
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/MainView.tsx
autonomous: true
requirements:
  - UX-01

must_haves:
  truths:
    - "Plugin UI says 'What to extract' instead of 'Extraction Scope'"
    - "Plugin UI says 'This element' instead of 'Single Node'"
    - "Plugin UI says 'This section' instead of 'Frame'"
    - "Plugin UI says 'Whole page' instead of 'Entire Page'"
    - "Plugin UI says 'layers' instead of 'nodes' everywhere"
    - "Plugin UI stats line shows 'N layers . N assets . ~NK tokens' (no colors, fonts, or auto-layout count)"
    - "Plugin UI token warning says 'This brief is large' without token count in the heading"
    - "Primary button label is shorter than 'Extract Design Brief'"
    - "Toast messages use 'layers' not 'nodes' and simplified stats"
  artifacts:
    - path: "src/views/MainView.tsx"
      provides: "Human-friendly terminology throughout plugin UI"
  key_links:
    - from: "src/views/MainView.tsx"
      to: "User-visible text"
      via: "Inline JSX string literals"
      pattern: "What to extract|This element|This section|Whole page|layers"
---

<objective>
Replace developer jargon throughout the plugin UI with human-friendly terminology, simplify stats displays, and rephrase the token warning in plain language.

Purpose: Non-technical users should understand every label in the plugin without knowing Figma internals or developer terminology. "Extraction Scope" and "Single Node" are meaningless to most users.

Output: Updated `MainView.tsx` with all terminology replacements applied consistently across labels, radio options, stats lines, toast messages, and warning banners.
</objective>

<execution_context>
@/Users/juliangalluzzo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juliangalluzzo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

@src/views/MainView.tsx

<interfaces>
<!-- Key types the executor needs. Extracted from codebase. -->

From src/views/MainView.tsx:
```typescript
// ExtractionStats interface (line 16-22):
interface ExtractionStats {
  frames: number;
  autoLayoutFrames: number;
  components: { name: string; count: number }[];
  textNodes: number;
  hiddenNodes: number;
}

// BriefStats (from src/brief/types.ts, used in MainView):
interface BriefStats {
  nodeCount: number;
  colorCount: number;
  fontCount: number;
  assetCount: number;
  estimatedTokens: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all UI terminology in MainView.tsx</name>
  <files>src/views/MainView.tsx</files>
  <action>
Apply the following changes to `src/views/MainView.tsx`. Each change is a specific string replacement at the identified location. After each change, verify no other occurrences of the old terminology remain in the file.

**Radio group label (line 480):**
- Old: `Extraction Scope`
- New: `What to extract`

**Radio option labels:**
- Line 491: `Single Node` -> `This element`
- Line 493-496: Hint text `Paste a URL with a node-id to use this option` -> `Select a specific element in Figma to use this option` (rephrased to plain language -- no "node-id")
- Line 506: `Frame` -> `This section`
- Line 516: `Entire Page` -> `Whole page`

**Large tree warning (line 536):**
- Old: `{largeTreeWarning.nodeCount} nodes detected`
- New: `{largeTreeWarning.nodeCount} layers detected`

**Extraction stats line (line 564):**
- Old: `{extractionResult.nodeCount} nodes &middot; {extractionStats.autoLayoutFrames} auto-layout frames &middot; {extractionStats.textNodes} text layers`
- New: `{extractionResult.nodeCount} layers &middot; {extractionStats.textNodes} text layers`
- This drops the auto-layout frames stat entirely per user decision.

**Brief ready stats line (lines 692-703):**
- Old: Shows nodeCount nodes, colorCount colors, fontCount fonts, assetCount assets, ~NK tokens
- New: Shows only three stats:
  ```tsx
  {briefResult.stats.nodeCount} layers &middot;{' '}
  {briefResult.stats.assetCount} assets &middot;{' '}
  <span style={{
    color: briefResult.stats.estimatedTokens > TOKEN_WARNING_THRESHOLD
      ? '#f59e0b'
      : 'inherit',
  }}>
    ~{Math.round(briefResult.stats.estimatedTokens / 1000)}K tokens
  </span>
  ```
  Remove the colorCount and fontCount display entirely.

**Token warning banner (lines 707-712):**
- Old:
  ```tsx
  <strong>Large brief (~{Math.round(briefResult.stats.estimatedTokens / 1000)}K tokens)</strong>
  <p>Recommended max: ~12K tokens. Consider selecting a smaller frame.</p>
  ```
- New:
  ```tsx
  <strong>This brief is large</strong>
  <p>Consider extracting a smaller section for better results.</p>
  ```
  Remove the dynamic token count from the heading. Remove the "Recommended max" technical language.

**Primary button label (line 738):**
- Old: `Extract Design Brief`
- New: `Get Brief`
- Keep the dynamic states unchanged: `Extracting...`, `Exporting assets...`, `Generating brief...`

**Toast messages:**
- Line 200 (brief ready toast):
  Old: `Brief ready: ${brief.stats.nodeCount} nodes, ${brief.stats.colorCount} colors, ${brief.stats.fontCount} fonts, ${brief.stats.assetCount} assets, ~${Math.round(brief.stats.estimatedTokens / 1000)}K tokens`
  New: `Brief ready: ${brief.stats.nodeCount} layers, ${brief.stats.assetCount} assets, ~${Math.round(brief.stats.estimatedTokens / 1000)}K tokens`
  (Drop colors and fonts from toast too -- match the simplified stats line)

- Line 365 (extraction toast):
  Old: `Extracted ${result.extraction.nodeCount} nodes`
  New: `Extracted ${result.extraction.nodeCount} layers`

- Line 403 (large tree confirm toast):
  Old: `Extracted ${pending.extraction.nodeCount} nodes`
  New: `Extracted ${pending.extraction.nodeCount} layers`

**ExtractionStats interface cleanup:**
- Remove `autoLayoutFrames` from the `ExtractionStats` interface (line 18). It is no longer displayed anywhere.
- Remove `if (node.autoLayout) stats.autoLayoutFrames++;` from the `collectStats` function (line 32).
- Remove `autoLayoutFrames: 0` from the stats initializer (line 25).
- This keeps the interface clean (YAGNI) and ensures no accidental future references to the dropped stat.

**Final audit:** After all changes, search the entire file for:
- "nodes" (should only appear in code identifiers like `rootNodes`, `nodeCount`, `textNodes`, `hiddenNodes`, `nodeId` -- NOT in user-visible strings)
- "Extraction Scope"
- "Single Node"
- "Entire Page"
- "auto-layout frames"
- "Extract Design Brief"
- "node-id" (in user-visible strings)
Verify NONE appear in user-visible text.
  </action>
  <verify>
    <automated>cd /Users/juliangalluzzo/Desktop/Projects/shipstudio-plugins/plugin-figma && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <done>
    - "What to extract" label replaces "Extraction Scope"
    - "This element" replaces "Single Node", "This section" replaces "Frame", "Whole page" replaces "Entire Page"
    - Disabled hint uses plain language (no "node-id")
    - All user-visible "nodes" replaced with "layers"
    - Stats line simplified to "layers . assets . tokens" (no colors, fonts, auto-layout)
    - Token warning says "This brief is large" without token count in heading
    - Primary button says "Get Brief"
    - All toast messages use "layers" and simplified stats
    - autoLayoutFrames removed from ExtractionStats interface
    - TypeScript compiles with no errors
    - All existing tests pass
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run` -- all tests pass
2. `npx tsc --noEmit` -- no type errors (confirms autoLayoutFrames removal is clean)
3. Manual grep for old terminology: `grep -n "Extraction Scope\|Single Node\|Entire Page\|auto-layout frames\|Extract Design Brief" src/views/MainView.tsx` returns no results
4. Manual grep for "nodes" in user-visible strings: verify only code identifiers remain
</verification>

<success_criteria>
- Zero instances of old jargon terminology in user-visible UI strings
- Stats displays show only layers, assets, and token estimate
- Token warning uses plain language without technical numbers
- Button label is "Get Brief"
- All toast messages are consistent with the new terminology
- TypeScript compilation succeeds
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-brief-instructions-terminology/06-02-SUMMARY.md`
</output>
