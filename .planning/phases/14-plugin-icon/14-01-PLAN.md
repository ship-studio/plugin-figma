---
phase: 14-plugin-icon
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/index.tsx]
autonomous: false
requirements: [POLISH-01]

must_haves:
  truths:
    - "The Ship Studio toolbar displays the Figma logo SVG icon next to the plugin name"
    - "The icon renders correctly at 14x14px and inherits the theme color"
  artifacts:
    - path: "src/index.tsx"
      provides: "Figma logo SVG inline in toolbar button"
      contains: "viewBox=\"0 0 24 24\" fill=\"currentColor\""
  key_links:
    - from: "src/index.tsx"
      to: "Ship Studio host CSS"
      via: "className toolbar-icon-btn and fill=currentColor"
      pattern: "fill=\"currentColor\""
---

<objective>
Replace the generic 4-squares grid icon in the Figma plugin toolbar button with the official Figma logo SVG.

Purpose: Give the plugin proper visual identity so users can identify it at a glance in the Ship Studio toolbar.
Output: Updated `src/index.tsx` with Figma logo SVG path.
</objective>

<execution_context>
@/Users/juliangalluzzo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juliangalluzzo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-plugin-icon/14-RESEARCH.md

<interfaces>
<!-- Current toolbar icon in src/index.tsx lines 158-178 -->
<!-- The button structure and className MUST be preserved. Only the <svg> contents change. -->

From src/index.tsx (lines 158-178):
```typescript
<button
  onClick={openModal}
  title="Figma Design Brief"
  className="toolbar-icon-btn"
>
  <svg
    width="14"
    height="14"
    viewBox="0 0 24 24"
    fill="none"
    stroke="currentColor"
    strokeWidth="2"
    strokeLinecap="round"
    strokeLinejoin="round"
  >
    <rect x="3" y="3" width="7" height="7" rx="1" />
    <rect x="14" y="3" width="7" height="7" rx="1" />
    <rect x="3" y="14" width="7" height="7" rx="1" />
    <rect x="14" y="14" width="7" height="7" rx="1" />
  </svg>
</button>
```

Replacement SVG (from Simple Icons, CC0 licensed):
```typescript
<svg
  width="14"
  height="14"
  viewBox="0 0 24 24"
  fill="currentColor"
>
  <path d="M15.852 8.981h-4.588V0h4.588c2.476 0 4.49 2.014 4.49 4.49s-2.014 4.491-4.49 4.491zM12.735 7.51h3.117c1.665 0 3.019-1.355 3.019-3.019s-1.355-3.019-3.019-3.019h-3.117V7.51zm0 1.471H8.148c-2.476 0-4.49-2.014-4.49-4.49S5.672 0 8.148 0h4.588v8.981zm-4.587-7.51c-1.665 0-3.019 1.355-3.019 3.019s1.354 3.02 3.019 3.02h3.117V1.471H8.148zm4.587 15.019H8.148c-2.476 0-4.49-2.014-4.49-4.49s2.014-4.49 4.49-4.49h4.588v8.98zM8.148 8.981c-1.665 0-3.019 1.355-3.019 3.019s1.355 3.019 3.019 3.019h3.117V8.981H8.148zM8.172 24c-2.489 0-4.515-2.014-4.515-4.49s2.014-4.49 4.49-4.49h4.588v4.441c0 2.503-2.047 4.539-4.563 4.539zm-.024-7.51a3.023 3.023 0 0 0-3.019 3.019c0 1.665 1.365 3.019 3.044 3.019 1.705 0 3.093-1.376 3.093-3.068v-2.97H8.148zm7.704 0h-.098c-2.476 0-4.49-2.014-4.49-4.49s2.014-4.49 4.49-4.49h.098c2.476 0 4.49 2.014 4.49 4.49s-2.014 4.49-4.49 4.49zm-.097-7.509c-1.665 0-3.019 1.355-3.019 3.019s1.355 3.019 3.019 3.019h.098c1.665 0 3.019-1.355 3.019-3.019s-1.355-3.019-3.019-3.019h-.098z" />
</svg>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace toolbar icon SVG with Figma logo</name>
  <files>src/index.tsx</files>
  <action>
In src/index.tsx, replace the inline SVG inside the toolbar button (lines ~163-177) with the Figma logo SVG from Simple Icons.

Specifically:
1. Keep the `<button>` wrapper exactly as-is (onClick, title, className unchanged)
2. Replace the entire `<svg>...</svg>` block (the 4-rect grid icon) with the Figma logo SVG
3. The new SVG MUST use: `width="14" height="14" viewBox="0 0 24 24" fill="currentColor"`
4. The new SVG MUST NOT have: `stroke`, `strokeWidth`, `strokeLinecap`, `strokeLinejoin`, or `fill="none"` -- the Figma logo is composed of filled shapes, not strokes
5. The SVG contains a single `<path>` element with the Figma logo path data (see interfaces block above for exact path data)

The exact replacement SVG is provided in the interfaces block above. Use that verbatim.
  </action>
  <verify>
    <automated>cd /Users/juliangalluzzo/Desktop/Projects/shipstudio-plugins/plugin-figma && npx tsc --noEmit 2>&1 | head -20 && grep -c 'fill="currentColor"' src/index.tsx && grep -c 'stroke="currentColor"' src/index.tsx</automated>
  </verify>
  <done>
- TypeScript compiles without errors
- src/index.tsx contains `fill="currentColor"` in the toolbar SVG (grep count >= 1)
- src/index.tsx does NOT contain `stroke="currentColor"` in the toolbar SVG (grep count = 0)
- The Figma logo path data is present in the file
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Figma logo renders correctly in toolbar</name>
  <files>src/index.tsx</files>
  <action>
Human verifies the Figma logo icon renders correctly in the Ship Studio toolbar.
  </action>
  <verify>Visual inspection by user</verify>
  <done>User confirms icon is visible, recognizable, properly sized, and theme-colored</done>
  <what-built>Replaced the generic 4-squares grid icon with the Figma logo in the Ship Studio toolbar button</what-built>
  <how-to-verify>
1. Start Ship Studio (or reload if already running)
2. Look at the toolbar at the top of the window
3. Find the Figma plugin button -- it should now show the recognizable Figma logo (the overlapping rounded rectangles/circles shape) instead of a 4-squares grid
4. Verify the icon is:
   - Visible and recognizable as the Figma logo
   - Properly sized (same as other toolbar icons like Vercel triangle)
   - Colored to match the toolbar theme (not a fixed color)
5. Click the button to confirm the plugin still opens normally
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual issues</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compilation succeeds (`npx tsc --noEmit`)
- The SVG in the toolbar button uses `fill="currentColor"` (not stroke)
- The Figma logo path data is present in `src/index.tsx`
- Visual verification: icon renders correctly in Ship Studio toolbar
</verification>

<success_criteria>
- The Ship Studio toolbar displays the Figma logo SVG icon at 14x14px
- The icon inherits the theme color via `fill="currentColor"`
- The plugin button still functions (opens modal on click)
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-plugin-icon/14-01-SUMMARY.md`
</output>
